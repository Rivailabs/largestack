# Security Policy

- Never hardcode secrets in source code. Use environment variables.
- Avoid building SQL queries with string formatting or concatenation. Use parameterized queries.
- All code must pass offline security checks before deployment.
