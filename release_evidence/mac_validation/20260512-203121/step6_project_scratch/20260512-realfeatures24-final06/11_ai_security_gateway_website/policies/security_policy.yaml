policy:
  name: security_policy
  version: 1.0
  rules:
    - id: deny-dangerous
      description: Deny dangerous delete tool
      action: deny
      tools:
        - dangerous_delete
    - id: allow-safe
      description: Allow safe tool
      action: allow
      tools:
        - safe_tool
