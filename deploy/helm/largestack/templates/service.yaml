apiVersion: v1
kind: Service
metadata:
  name: {{ include "largestack.fullname" . }}
  labels:
    {{- include "largestack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "largestack.selectorLabels" . | nindent 4 }}
