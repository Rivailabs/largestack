Largestack AI v{{ .Chart.AppVersion }} installed.

1. Get the dashboard URL:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/
{{- end }}
{{- else if eq .Values.service.type "LoadBalancer" }}
  export SERVICE_IP=$(kubectl get svc -n {{ .Release.Namespace }} {{ include "largestack.fullname" . }} -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
  echo http://$SERVICE_IP:{{ .Values.service.port }}/
{{- else }}
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "largestack.fullname" . }} 8787:{{ .Values.service.port }}
  echo http://localhost:8787/
{{- end }}

2. Authenticate with X-API-Key:
   curl -H "X-API-Key: $YOUR_KEY" http://.../api/metrics

3. View pod status:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if gt (int .Values.replicaCount) 1 }}
{{- if eq .Values.env.LARGESTACK_RATE_LIMIT_BACKEND "inprocess" }}

WARNING: replicaCount={{ .Values.replicaCount }} but rate limiter is in-process.
         Each pod has its own state — global limit = N × per-pod limit.
         Set env.LARGESTACK_RATE_LIMIT_BACKEND=redis and provide secrets.redisUrl
         for distributed enforcement.
{{- end }}
{{- end }}
