# Providers

Providers are selected with strings such as `openai/gpt-4o-mini`, `deepseek/deepseek-chat`, `anthropic/claude-*`, and local `ollama/*` models. Live provider E2E requires fresh provider keys in environment variables. Rotate any key that was pasted into chat or logs before running validation.
