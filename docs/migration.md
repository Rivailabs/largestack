# Migration

Use `largestack migrate check` to inspect a project and `largestack migrate apply` for safe idempotent migrations. Individual helpers are available for `config`, `memory`, and `traces` artifacts with an explicit `--write` flag.
